export type Shop = {
    id: string
    name: string
    area: string
    rating: number
    reviewCount: number
    tags: string[]
    description: string
    coverImage: string
    leadMessage: string
}